import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function DashboardPage() {
  return (
    <PlaceholderPanel
      title="Dashboard"
      description="This area will summarize the signed-in user's active projects, recent time entries, and report totals."
    />
  );
}
